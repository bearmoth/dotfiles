# zoxide is deliberately absent: it's a yazi builtin (like fzf), and
# "yazi-rs/plugins:zoxide" never existed in that monorepo — newer `ya pkg`
# fails hard trying to deploy it. keymap.toml's `plugin zoxide` uses the
# builtin.

[[flavor.deps]]
use = "yazi-rs/flavors:catppuccin-mocha"
rev = "0670801"
hash = "7dfe41c9c3d9be76da8a844263d0bbed"
