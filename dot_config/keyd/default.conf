[ids]
*

[main]
# Tap caps_lock alone → Escape
# Hold caps_lock + any key → Hyper (Ctrl+Alt+Shift+Super)
# Uses overload() not overloadt(): decision is based on next keypress,
# not elapsed time — zero latency, no flakiness.
capslock = overload(hyper, esc)

[hyper:C-A-S-M]
