{{ if eq .chezmoi.os "linux" -}}
#!/bin/bash
set -euo pipefail

# Re-run weekly: {{ now.Year }}-W{{ div now.YearDay 7 }}
# Intentionally a no-op: `herdr update` fails from inside an attached herdr
# session, so running it during `chezmoi apply` makes apply flaky. Update
# herdr manually outside herdr instead.
exit 0
{{ end -}}
